# ⚠ QUESTO FILE SI ESTENDE, NON SI RISCRIVE (mai «>», sempre in coda): il 18/9/2026 una riscrittura ha tolto
#   la riga «rebalix/» e lo snapshot ha provato a spingere su GitHub i dump del database e la password.
# dump del DATABASE: contengono dati utente e credenziali → restano SOLO locali (il DB vero è su Supabase)
rebalix/
# cestino di file temporanei spostati dalle pulizie: non è archivio
rebalix-cestino/
# symlink verso ~/Documents (git non ne salva il contenuto): è specchiato in _whitelist-docs-mirror/
rebalix-whitelist-docs
# rumore
.DS_Store
_push-archivi.log
# log operativi delle macchine: non sono archivio
_backup-b2.log
_censimento.log
# depositi che hanno la loro copia remota su B2, non nel git degli archivi
rebalix-baseline/
rebalix-edizioni/
rebalix-fonti-su-richiesta/
rebalix-arca-fonti/
# mai credenziali, ovunque
.env
.env.*
